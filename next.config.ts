import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com','aws-nightlife.s3.eu-north-1.amazonaws.com'],
  },

};

export default nextConfig;
